POSTGRES_DB=harvest_db
POSTGRES_USER=harvest
POSTGRES_PASSWORD=changeme
DATABASE_URL=postgresql://harvest:changeme@localhost:5432/harvest_db
# Shared key for GET /api/widget (dashboard widget feed).
# Generate one with: openssl rand -hex 32
# Leave empty to keep the route disabled (it answers 503 without a key).
WIDGET_TOKEN=
